replicaCount: 1

image:
  repository: overult01/mcp-license-analyzer
  tag: "0.1.0"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

resources: {}
readinessProbe:
  enabled: false
